import { Story, Meta } from '@storybook/react/types-6-0';

import {{pascalCase name}} from '.';

export default {
  title: '{{pascalCase name}}',
  component: {{pascalCase name}},
} as Meta;

export const Template: Story = () => <{{pascalCase name}} />;

export const Primary = Template.bind({});
Primary.args = {
  label: 'Button',
};
