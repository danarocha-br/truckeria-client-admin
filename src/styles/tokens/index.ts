import * as allColors from './colors';
import allFont from './font';
import allRadii from './radii';
import * as allShadow from './shadow';
import allSpacing from './spacing';

export const colors = { ...allColors };
export const font = { ...allFont };
export const radii = { ...allRadii };
export const shadow = { ...allShadow };
export const spacing = { ...allSpacing };
